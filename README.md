# Wireguard tools

### Description
API for handling wireguard clients.\
Fast API and Wireguard are running in docker containers.

### API Specification
see [spec.yml](server/api/spec.yaml)

### Deploy
```bash
docker compose up --build -d
```